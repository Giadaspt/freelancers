<?php

use Illuminate\Database\Seeder;
use App\Message;
use App\User;

class UpdateMessagesTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $messages = Message::all();

        // foreach($messages as $message){

        //     $data=['user_id'=>User::inRandomOrder()->first()->id];

        //     $message->update($data);
        // }
    }
}
